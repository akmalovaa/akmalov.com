// Authors are rendered at the bottom of each blog post (see BlogPostItem.tsx)
// and hidden on the blog list page, so this header slot always renders nothing.
export default function AuthorsWrapper(): null {
  return null;
}
